---
http:
  # -- Change Router Configuration here...
  routers:
    koalakeys:
      rule: "Host(`koalakeys.l.zzzealed.com`)"  # <-- Change Rules here...
      service: koalakeys  # <-- Change Service Name here...
      priority: 1000  # <-- (Optional) Change Routing Priority here...
      entryPoints:
        - web
        - websecure
      tls:
        certResolver: cloudflare

  # -- Change Service Configuration here...
  services:
    koalakeys:  # <-- Change Service Name here...
      loadBalancer:
        servers:
          - url: "http://192.168.0.118:8282"  # <-- Change Target Service URL here...
