---
http:
  # -- Change Router Configuration here...
  routers:
    dashdot:
      rule: "Host(`dashdot.l.zzzealed.com`)"  # <-- Change Rules here...
      service: dashdot  # <-- Change Service Name here...
      priority: 1000  # <-- (Optional) Change Routing Priority here...
      entryPoints:
        - web
        - websecure
      tls:
        certResolver: cloudflare

  # -- Change Service Configuration here...
  services:
    dashdot:  # <-- Change Service Name here...
      loadBalancer:
        servers:
          - url: "http://192.168.0.118:3001"  # <-- Change Target Service URL here...
