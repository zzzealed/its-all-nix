---
http:
  # -- Change Router Configuration here...
  routers:
    glance:
      rule: "Host(`glance.l.zzzealed.com`)"  # <-- Change Rules here...
      service: glance  # <-- Change Service Name here...
      priority: 1000  # <-- (Optional) Change Routing Priority here...
      entryPoints:
        - web
        - websecure
      tls:
        certResolver: CertificateResolver0

  # -- Change Service Configuration here...
  services:
    glance:  # <-- Change Service Name here...
      loadBalancer:
        servers:
          - url: "http://server.l.zzzealed.com:50596"  # <-- Change Target Service URL here...
