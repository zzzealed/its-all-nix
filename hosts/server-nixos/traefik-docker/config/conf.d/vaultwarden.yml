---
http:
  # -- Change Router Configuration here...
  routers:
    vaultwarden:
      rule: "Host(`vault.l.zzzealed.com`)"  # <-- Change Rules here...
      service: vaultwarden  # <-- Change Service Name here...
      priority: 1000  # <-- (Optional) Change Routing Priority here...
      entryPoints:
        - web
        - websecure
      tls:
        certResolver: CertificateResolver0

  # -- Change Service Configuration here...
  services:
    vaultwarden:  # <-- Change Service Name here...
      loadBalancer:
        servers:
          - url: "http://server.l.zzzealed.com:50847"  # <-- Change Target Service URL here...
