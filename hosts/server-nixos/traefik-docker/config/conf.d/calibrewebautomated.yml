---
http:
  # -- Change Router Configuration here...
  routers:
    calibrewebautomated:
      rule: "Host(`calibre.l.zzzealed.com`)"  # <-- Change Rules here...
      service: calibrewebautomated # <-- Change Service Name here...
      priority: 1000  # <-- (Optional) Change Routing Priority here...
      entryPoints:
        - web
        - websecure
      tls:
        certResolver: CertificateResolver0

  # -- Change Service Configuration here...
  services:
    calibrewebautomated:  # <-- Change Service Name here...
      loadBalancer:
        servers:
          - url: "http://server.l.zzzealed.com:50466"  # <-- Change Target Service URL here...
