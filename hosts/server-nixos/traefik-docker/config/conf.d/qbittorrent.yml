---
http:
  # -- Change Router Configuration here...
  routers:
    qbittorrent:
      rule: "Host(`qbittorrent.l.zzzealed.com`)"  # <-- Change Rules here...
      service: qbittorrent  # <-- Change Service Name here...
      priority: 1000  # <-- (Optional) Change Routing Priority here...
      entryPoints:
        - web
        - websecure
      tls:
        certResolver: cloudflare

  # -- Change Service Configuration here...
  services:
    qbittorrent:  # <-- Change Service Name here...
      loadBalancer:
        servers:
          - url: "http://192.168.0.118:8090"  # <-- Change Target Service URL here...
