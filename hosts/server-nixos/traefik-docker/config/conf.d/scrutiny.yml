---
http:
  # -- Change Router Configuration here...
  routers:
    scrutiny:
      rule: "Host(`scrutiny.l.zzzealed.com`)"  # <-- Change Rules here...
      service: scrutiny  # <-- Change Service Name here...
      priority: 1000  # <-- (Optional) Change Routing Priority here...
      entryPoints:
        - web
        - websecure
      tls:
        certResolver: CertificateResolver0

  # -- Change Service Configuration here...
  services:
    scrutiny:  # <-- Change Service Name here...
      loadBalancer:
        servers:
          - url: "http://server.l.zzzealed.com:50236"  # <-- Change Target Service URL here...
