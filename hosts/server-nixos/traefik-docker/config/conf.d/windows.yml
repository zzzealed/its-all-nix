---
http:
  # -- Change Router Configuration here...
  routers:
    windows:
      rule: "Host(`win.l.zzzealed.com`)"  # <-- Change Rules here...
      service: windows  # <-- Change Service Name here...
      priority: 1000  # <-- (Optional) Change Routing Priority here...
      entryPoints:
        - web
        - websecure
      tls:
        certResolver: cloudflare

  # -- Change Service Configuration here...
  services:
    windows:  # <-- Change Service Name here...
      loadBalancer:
        servers:
          - url: "http://192.168.0.118:8006"  # <-- Change Target Service URL here...
