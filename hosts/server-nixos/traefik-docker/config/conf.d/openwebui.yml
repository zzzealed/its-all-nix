---
http:
  # -- Change Router Configuration here...
  routers:
    openwebui:
      rule: "Host(`chat.l.zzzealed.com`)"  # <-- Change Rules here...
      service: openwebui  # <-- Change Service Name here...
      priority: 1000  # <-- (Optional) Change Routing Priority here...
      entryPoints:
        - web
        - websecure
      tls:
        certResolver: CertificateResolver0

  # -- Change Service Configuration here...
  services:
    openwebui:  # <-- Change Service Name here...
      loadBalancer:
        servers:
          - url: "http://server.l.zzzealed.com:50112"  # <-- Change Target Service URL here...
